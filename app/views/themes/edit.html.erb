<%= simple_form_for [ @theme ] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :information %>
  <%= f.input :photo, as: :file %>

  <%= f.submit "Save changes", class: "btn btn-primary" %>
<% end %>
